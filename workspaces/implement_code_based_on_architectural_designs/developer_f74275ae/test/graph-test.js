// graph-test.js
const { addEdge, detectCycle } = require('../src/utils/Graph');

test('should return true if adding an edge creates a cycle', () => {
  // Test cases for cycle detection
});
test('should detect cycles in the graph', () => {
  // More test cases
});